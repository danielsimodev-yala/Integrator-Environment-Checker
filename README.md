# Integrator-Environment-Checker
Python-based diagnostic utility for hardware integrators. Automatically verifies COM ports (ESP32/Arduino), camera availability, and network status to ensure the workstation is ready for development.
